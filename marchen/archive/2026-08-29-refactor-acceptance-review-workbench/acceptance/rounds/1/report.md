本轮按已确认决策实现：不增加逐项已查看状态；1280px 及以上常驻当前项检查器，以下进入 Sheet；全屏 Dialog 保留多图缩略图，不增加缩放工具栏。

浏览器自动化覆盖 1440px、1024px、768px、375px，均未出现横向溢出。浏览器安全策略禁止直接导航 `file://`，因此没有伪造该项截图；协议写入门禁改由纯逻辑测试证明 `file:` 与未知协议不可写，历史轮次及 accepted、rejected 状态则在浏览器中确认不渲染表单。
