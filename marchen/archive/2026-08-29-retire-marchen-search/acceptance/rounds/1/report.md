# 第 1 轮验收报告

本轮在独立临时工作区使用构建后的 CLI 手工走通真实 TTY 初始化、同版本配置迁移、跨版本配置与托管 Skill 迁移、旧命令失败以及变更归档。临时工作区保留了一个固定内容的 `.search/index.sqlite` 哨兵；迁移和归档前后哈希完全一致，证明运行路径没有删除或改写遗留索引。

CLI 与 Skill 属于文本交互面，本轮以真实命令输出、生成配置和最终文件内容作为观察结果，不编造浏览器截图。全局 `~/.cache/qmd/models/` 未被读取、写入或删除；README 只把它列为用户确认无其他消费者后才能手动清理的共享缓存。

实现与当前工作区中已经存在的 Idea Capture、acceptance UI 和归档改动共存；搜索退役只清理相应链路，没有回滚这些并行成果，也没有改写历史 changelog/archive 记录。
