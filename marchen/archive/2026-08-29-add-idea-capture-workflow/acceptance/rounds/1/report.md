# 第 1 轮验收报告

本轮在独立临时工作区使用构建后的 CLI 手工走通 Idea 创建、轻量列举、完整读取、受 revision 保护更新、陈旧 revision 拒绝、晋升和随 change 归档。临时工作区已在取证后删除，没有把示例 Idea 写入当前项目。

CLI 验收面以命令输出和最终文件位置为证据，因此不编造浏览器截图。Idea 默认可被 Git 跟踪；Capture 会清理明显凭据和本机绝对路径，但不会声称能自动识别任意组织机密，提交前仍需人工 review。
